Given /^a new server has been provisioned$/ do
  server_create("<%= @project %>")
end

And /^the <%= @project %> role has been applied$/ do
  chef_set_client_attributes("<%= @project %>", :run_list => ["role[<%= @project %>]"])
end

And /^Chef has been run$/ do
  chef_run_client("<%= @project %>")
end

Given /^some initial state$/ do
  pending # express the regexp above with the code you wish you had
end

When /^tickle$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^resulting state$/ do
  pending # express the regexp above with the code you wish you had
end
