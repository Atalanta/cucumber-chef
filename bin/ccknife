#!/usr/bin/env ruby
require 'pathname'

knife_rb = Pathname.new(File.join(Dir.pwd, ".cucumber-chef/knife.rb")).expand_path
command = "/usr/bin/env knife #{ARGV.join(" ")} -c #{knife_rb}"
%x(#{command})
